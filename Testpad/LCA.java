        // if (root == null) return -1;
        // if (root.data == k1 || root.data == k2) return root.data;

        // int left = lowestCommonAncestor(root.left, k1, k2);
        // int right = lowestCommonAncestor(root.right, k1, k2);

        // if (left != -1 && right != -1) return root.data;
        // return (left != -1) ? left : right;